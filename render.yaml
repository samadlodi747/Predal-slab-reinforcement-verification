services:
  - type: web
    name: predal-verifier
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 2 --threads 4 --timeout 180
    healthCheckPath: /healthz
    autoDeploy: true
